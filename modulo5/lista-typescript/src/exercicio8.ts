console.log("Exercício 8")

/* function fatorial(palavra: string) : number {
    if (palavra.length < 0)
      return -1;
    else if (palavra.length == 0)
      return 1;
    else {
      return (palavra.length * fatorial(palavra.length - 1));
    }
  }

console.log(fatorial("julia")) */

